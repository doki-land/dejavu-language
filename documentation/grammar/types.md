# 值模型实现状态

当前 Dejavu 契约使用 JSON 对齐的运行时值：null、布尔值、数字、字符串、列表和对象。宿主可以用原生类型承载这些值，但序列化和求值语义必须与
IR/Context 契约一致。

当前 Contract 不定义模板内静态类型标注、整数位宽、数组长度类型、切片、元组类型、类型推断或编译期类型检查。因此 Dejavu
不能在用户文档中宣传为强类型模板语言。

安全 HTML 是渲染边界上的显式标记，不是通用静态类型系统。实现必须确保普通字符串默认转义，只有明确的安全值或 `safe` / `raw`
才绕过转义。
