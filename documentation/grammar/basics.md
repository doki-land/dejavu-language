# 模板表面实现状态

当前契约定义普通文本、默认代码定界符 `<%` / `%>`、默认注释定界符 `<#` / `#>`，以及通过 `Language.template` 配置自定义定界符的行为。

实现必须验证：

- 模板外文本按原字节顺序进入 IR。
- 注释不进入渲染输出。
- 插值与语句共享代码定界符，由 parser 根据内容区分。
- 自定义定界符只改变词法边界，不改变表达式和控制流语义。

用户示例见外部文档；核心证据见 `specifications/conformance/t1` 和 inheritance 中的自定义定界符 fixture。
