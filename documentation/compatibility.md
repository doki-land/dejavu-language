# 宿主实现状态

本页供核心开发者维护，不是语言规范。状态必须包含验证日期、命令和 fixture 范围。

截至 2026-08-11，`pnpm conformance` 的基础 T1 用例在 TypeScript、Rust、C#、Python 上有输出，但 `safe_raw` 在 runner
中没有完成，Kotlin 在观察环境中被跳过。因此当前不能宣称完整跨宿主 conformance。

| 宿主       | T1 IR          | inheritance / loader | 状态     |
|------------|----------------|----------------------|----------|
| TypeScript | 基础用例有证据 | engine 测试覆盖      | 部分验证 |
| Rust       | 基础用例有证据 | 未由该命令确认       | 部分验证 |
| C#         | 基础用例有证据 | 未由该命令确认       | 部分验证 |
| Python     | 基础用例有证据 | 未由该命令确认       | 部分验证 |
| Kotlin     | 观察环境跳过   | 未验证               | 未验证   |

runner 在所有宿主跳过某个用例时仍可能返回成功；这属于工具问题，不能作为文档能力证明。更新此页时，优先引用具体 fixture
和命令，不引用泛化的“全平台一致”。
